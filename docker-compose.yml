services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development  # Changé pour le développement
      - JWT_SECRET=your-super-secret-jwt-key-change-in-production
      - PORT=3000
    volumes:
      - ./backend/database:/app/backend/database
      # Volumes pour le développement - synchronisation en temps réel
      - ./backend:/app/backend
      - ./frontend:/app/frontend
      # Exclure node_modules pour éviter les conflits
      - /app/backend/node_modules
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/auth/health', (res) => { process.exit(res.statusCode === 200 ? 0 : 1) }).on('error', () => process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s


